# Short for Codex

`short` v2는 AISUPPORT가 직접 관리하는 항상 실행 Skill입니다. Caveman의 간결하고 정확한 표현 원칙과 Ponytail의 작고 유지 가능한 구현 원칙을 하나의 짧은 정책으로 다시 작성하고, 실제 사용 내역에서 확인된 반복 문제를 범용 규칙으로 보강했습니다.

## 작동 범위

- 모든 응답: 군더더기와 반복을 줄이되 부정 표현, 숫자, 단위, 경로, 명령어, 코드·API 이름, 오류 원문을 보존합니다. 비밀값이나 민감값이 포함되면 해당 값만 가리고 주변 증거는 보존합니다.
- 도구 출력: 필요한 파일·범위·오류만 읽고 성공 로그의 반복을 줄이되, 실패 명령·종료 상태·진단 문맥은 보존합니다. 보존하는 모든 항목에서도 비밀값과 민감값만 가립니다.
- 코딩 작업: 관련 흐름을 먼저 확인하고 기존 코드, 표준 라이브러리, 플랫폼 기본 기능, 설치된 의존성 순서로 재사용합니다.
- 위험한 작업: 보안, 삭제, 복구 불가능한 변경, 순서가 중요한 절차는 축약보다 명확성과 안전을 우선합니다.
- 저장되는 글: 대상 파일 형식과 프로젝트 관례를 먼저 따릅니다. 문서, 코드 주석, 커밋, 리뷰, 외부 메시지처럼 일반 문장이 맞는 곳에서는 완전한 문장으로 작성합니다.

다음 우선순위를 따릅니다.

```text
사용자 요구와 저장소 규칙 > 안전과 정확성 > 명확성과 유지보수성 > 최소 변경 > 짧은 표현
```

`short`는 가장 적은 줄 수를 목표로 하지 않습니다. 요청 전체를 만족하는 가장 작고 명확한 결과를 목표로 합니다. 기존 프로젝트의 테스트 체계를 그대로 사용하며 테스트 개수나 프레임워크를 임의로 제한하지 않습니다.

## 사용 내역 기반 규칙

- 정보만 요청한 경우 실제 상태를 변경하지 않습니다. 실행 의도·대상·범위가 명확하면 불필요하게 다시 묻지 않고, 결과에 영향을 주는 모호함만 한 번 확인합니다.
- 사용자의 최신 지시와 기존 지시가 충돌하면 충돌 부분만 바꾸고 나머지 조건은 유지합니다. 현재 승인된 범위 밖의 완료 작업은 명시적인 요청 없이 자동으로 되돌리지 않습니다.
- 비밀값은 승인된 목적에 필요한 최소 범위에서만 사용하며 그 밖으로 출력·기록·전달하지 않습니다.
- 승인된 대상과 범위만 변경하고 범위 밖 내용과 사용자의 기존 변경을 보존합니다. 올바른 해결에 범위 확대가 필요하면 먼저 설명하고 승인을 받습니다.
- 실패·차단·부분 완료·검증 불가 상태를 완료로 표현하지 않습니다. 실제 변경과 검증 상태, 원인, 다음 조치를 짧게 보고합니다.

이 규칙들은 모든 프로젝트에 적용되는 일반 원칙입니다. 구체적인 코드·문서·데이터 도구와 사용자 표현은 정책 검증 사례로만 사용하고 Skill 본문에는 고정하지 않습니다. 별도 `short-guard` Hook, 자동 공식 출처 검색, 일반 외부 알림 내용 마스킹은 이 버전에 포함하지 않습니다.

## RTK 방식 통합

RTK의 유용한 원칙인 좁은 명령, 대상 범위 지정, 성공 출력 요약을 Short의 지침 수준 규칙으로 흡수했습니다. Short는 Skill 지침이므로 도구가 반환한 원시 출력을 대화 문맥에 들어오기 전에 가로채거나 자동 압축하지는 못합니다. 별도 RTK 실행 파일, Hook, 백그라운드 프로세스, 텔레메트리는 설치하지 않습니다.

## 사용

`lite`, `full`, `ultra`는 Codex의 실제 런타임 모드가 아니라 응답 방식을 정하는 대화용 지시어입니다. 기본값은 `short full`이며 새 작업에서 자동으로 시작합니다.

```text
short lite로 진행해줘.
short full로 진행해줘.
short ultra로 진행해줘.
stop short
normal mode
```

- `lite`: 완전한 문장을 유지하면서 군더더기를 줄입니다.
- `full`: 표현과 구현 규칙을 모두 적용하는 기본 단계입니다.
- `ultra`: 선택적인 설명·예시·반복된 성공 출력을 더 강하게 줄입니다.
- 어떤 단계도 요청된 구현, 작업 범위, 안전, 정확성, 필요한 오류 처리와 검증을 줄이지 않습니다.
- `stop short`, `short off`, `normal mode`: 현재 작업의 남은 동안 비활성화하며 새 작업은 다시 `short full`로 시작합니다.

명시적으로 호출할 때는 `$short`를 사용할 수 있습니다.

## 설치와 이전 버전 정리

일반 AISUPPORT 설치가 다음 항목을 함께 처리합니다.

1. 저장소에 고정된 `short` 파일의 무결성을 확인합니다.
2. `short`를 사용자 Skill 폴더에 설치합니다.
3. 이전 Caveman·Ponytail Skill 설치본을 활성 폴더에서 제거하고 `skill-backups` 폴더로 이동합니다.
4. 기존 Caveman 항상 실행 블록을 Short 블록으로 바꿉니다.
5. 설치 결과와 이전 Skill 부재를 다시 검증합니다.

백업은 충돌이나 사용자 수정 내용을 잃지 않기 위한 복구용입니다. Codex를 다시 시작하면 `short`가 새 작업부터 항상 적용됩니다.

## 출처와 유지보수

`short`는 AISUPPORT의 독립 Skill이므로 Caveman이나 Ponytail의 새 버전을 자동 병합하지 않습니다. 사용 내역에서 확인된 반복 문제만 일반 규칙으로 반영하고 구체적인 작업 예시는 테스트로 분리합니다. `tests/short-behavior-contract.json`은 기대 행동을 기록한 정적 계약이며 실제 모델이나 API 응답을 실행·채점하지 않습니다. 테스트는 이 계약의 구조와 Skill 정책 문구의 연결만 결정론적으로 확인합니다. 원본 아이디어의 라이선스와 출처는 `THIRD_PARTY_NOTICES.md`에 유지합니다.
