# Short for Codex

`short`는 AISUPPORT가 직접 관리하는 항상 실행 Skill입니다. Caveman의 간결하고 정확한 표현 원칙과 Ponytail의 작고 유지 가능한 구현 원칙을 하나의 짧은 정책으로 다시 작성했습니다.

## 작동 범위

- 모든 응답: 군더더기와 반복을 줄이되 부정 표현, 숫자, 단위, 경로, 명령어, 코드·API 이름, 오류 원문을 보존합니다.
- 코딩 작업: 관련 흐름을 먼저 확인하고 기존 코드, 표준 라이브러리, 플랫폼 기본 기능, 설치된 의존성 순서로 재사용합니다.
- 위험한 작업: 보안, 삭제, 복구 불가능한 변경, 순서가 중요한 절차는 축약보다 명확성과 안전을 우선합니다.
- 저장되는 글: 문서, 코드 주석, 커밋, 리뷰, 메시지는 완전한 일반 문장으로 작성합니다.

다음 우선순위를 따릅니다.

```text
사용자 요구와 저장소 규칙 > 안전과 정확성 > 명확성과 유지보수성 > 최소 변경 > 짧은 표현
```

`short`는 가장 적은 줄 수를 목표로 하지 않습니다. 요청 전체를 만족하는 가장 작고 명확한 결과를 목표로 합니다. 기존 프로젝트의 테스트 체계를 그대로 사용하며 테스트 개수나 프레임워크를 임의로 제한하지 않습니다.

## 사용

기본값은 `short full`이며 새 작업에서 자동으로 시작합니다.

```text
short lite로 진행해줘.
short full로 진행해줘.
short ultra로 진행해줘.
stop short
normal mode
```

- `lite`: 완전한 문장을 유지하면서 간결하게 답합니다.
- `full`: 표현과 구현 규칙을 모두 적용하는 기본 단계입니다.
- `ultra`: 선택 사항과 반복을 더 강하게 줄이지만 범위, 명확성, 안전, 검증은 줄이지 않습니다.
- `stop short`, `short off`, `normal mode`: 현재 작업에서 비활성화합니다.

명시적으로 호출할 때는 `$short`를 사용할 수 있습니다.

## 설치와 이전 버전 정리

일반 AISUPPORT 설치가 다음 항목을 함께 처리합니다.

1. 저장소에 고정된 `short` 파일의 무결성을 확인합니다.
2. `short`를 사용자 Skill 폴더에 설치합니다.
3. 이전 Caveman·Ponytail Skill 설치본을 활성 폴더에서 제거하고 `skill-backups` 폴더로 이동합니다.
4. 기존 Caveman 항상 실행 블록을 Short 블록으로 바꿉니다.
5. 설치 결과와 이전 Skill 부재를 다시 검증합니다.

백업은 충돌이나 사용자 수정 내용을 잃지 않기 위한 복구용입니다. Codex를 다시 시작하면 `short`가 새 작업부터 항상 적용됩니다.

## 출처와 유지보수

`short`는 AISUPPORT의 독립 Skill이므로 Caveman이나 Ponytail의 새 버전을 자동 병합하지 않습니다. 향후 사용 내역을 검토해 실제로 반복되는 문제만 규칙에 반영합니다. 원본 아이디어의 라이선스와 출처는 `THIRD_PARTY_NOTICES.md`에 유지합니다.
