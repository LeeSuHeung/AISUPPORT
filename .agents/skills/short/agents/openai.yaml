interface:
  display_name: "Short"
  short_description: "Concise answers, code, and tool output"
  default_prompt: "Use $short to produce the smallest clear, correct result with focused tool output."

policy:
  allow_implicit_invocation: true
